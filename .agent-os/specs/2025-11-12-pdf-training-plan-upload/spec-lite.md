# Spec Summary (Lite)

Extend training plan upload to support PDF files alongside existing CSV uploads by parsing PDFs in Node.js, converting extracted text to CSV format using an LLM-based conversion service, and validating the output before storage. This eliminates manual CSV conversion for users who have training plans in PDF format, improving onboarding experience while maintaining backward compatibility with existing CSV uploads.
