# Spec Summary (Lite)

Implement Strava OAuth authentication to allow users to log in with their Strava accounts and access their training data. Users will see a login page with a "Login with Strava" button that initiates the OAuth flow, and after successful authentication, they'll land on a dashboard displaying their profile and recent activities from the past 30 days.
