# Spec Summary (Lite)

Display uploaded training plans in a tabular format on the Dashboard when an active plan exists. The current week displays prominently in an expanded table, with other weeks accessible via collapsible sections. Backend API will be enhanced to include CSV content, which the frontend parses dynamically to render a flexible table structure using shadcn/ui components.
