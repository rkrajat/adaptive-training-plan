# Spec Summary (Lite)

Implement a weekly runs report card that displays aggregated Strava activity metrics (total distance, run count, average pace, total time, longest run) for the current training week at the top of the dashboard. The component automatically calculates which week to display based on the training plan's start date and provides runners with at-a-glance visibility into their weekly training progress.
