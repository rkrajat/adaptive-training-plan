# Spec Summary (Lite)

Implement dark mode theme toggle in the header navigation using shadcn/ui and Tailwind CSS. The feature detects OS-level theme preference for first-time visitors, allows manual toggling between light/dark modes, and persists user preference in localStorage across sessions. Prevents theme flash on page load through early script injection.
